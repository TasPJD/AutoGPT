# HarLin — NEXUS Market-Readiness Package (2026-07-06)

Session deliverable: holistic market-readiness upgrade for the NEXUS suite, in priority order
set by Paul on 2026-07-06:

1. **GeoLedger** — full deep-dive (`geoledger/`): semantic iteration v0.6 plan, log/failure
   analysis, modularisation architecture, vector/semantic layer, multi-user concurrency,
   security, IP protection, UI/UX, market positioning + launch checklist, plus reference
   implementations under `geoledger/reference-impl/`.
2. **FieldCam** (`fieldcam/`)
3. **PlanView** (`planview/`)
4. **Vitrine** (`vitrine/`)
5. **Core Tray recognition & renaming** (`coretray/`)

Key documents are mirrored to `C:/AI/AI_Projects/NEXUS.prj/<project>.prj/Docs/` via the
HarLin MCP so local sessions can execute against them. The reference implementations here are
integration-ready Node modules written against GeoLedger's documented schema and conventions;
they are the starting point for the local build, not a parallel fork.

All analysis is grounded in the project records at `C:/AI/AI_Projects/NEXUS.prj/` (PROGRESS,
CHANGELOG, ISSUES, architecture docs) as at 2026-07-06.
